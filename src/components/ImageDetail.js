import React from "react";
import { View, Text, StyleSheet } from 'react-native';

const ImageDetail = () => {
    return (
        <View>
            <Text>Show other images</Text>
        </View>
    )
}

const styles = StyleSheet.create({})

export default ImageDetail;